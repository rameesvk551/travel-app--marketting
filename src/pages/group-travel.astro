---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const groupFeatures = [
  { icon: '📨', title: 'Invite via Link', desc: 'Share a simple link — no signup required to view. Collaborators join in one click.' },
  { icon: '✏️', title: 'Real-Time Editing', desc: 'Everyone sees changes instantly. Add suggestions, modify plans, leave comments.' },
  { icon: '🗳️', title: 'Vote on Activities', desc: 'Can\'t decide between paragliding and kayaking? Let the group vote and settle it fairly.' },
  { icon: '💬', title: 'Built-In Chat', desc: 'Discussion threads for each day. No more scattered WhatsApp messages.' },
  { icon: '💳', title: 'Smart Cost Splitting', desc: 'Track expenses and split costs automatically. Settle up at the end of the trip.' },
  { icon: '👑', title: 'Organizer Controls', desc: 'The trip captain approves changes and locks confirmed bookings.' },
];
---

<BaseLayout title="Group Travel" description="Plan group trips without the chaos. Invite friends, vote on activities, split costs, and collaborate in real-time.">
  <Header />
  <main>
    <section class="pt-32 pb-16 bg-gradient-to-b from-primary-500 to-teal-600 text-white">
      <div class="container-narrow text-center">
        <div class="inline-flex items-center gap-2 bg-white/10 rounded-full px-4 py-2 mb-6 text-sm">
          <span>👥</span> Made for Groups
        </div>
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-display font-extrabold mb-6">
          Group Trips, <span class="text-accent-300">Zero Chaos</span>
        </h1>
        <p class="text-xl text-white/80 max-w-2xl mx-auto mb-8">
          No more WhatsApp groups, Google Sheets, and "who's paying for what?" arguments. Plan together, book together.
        </p>
        <a href="/#hero-input" class="btn-primary">Plan a Group Trip</a>
      </div>
    </section>

    <section class="section-padding bg-white">
      <div class="container-narrow">
        <h2 class="text-3xl font-display font-bold text-dark-500 mb-4 text-center">Collaboration Tools</h2>
        <p class="text-center text-gray-500 mb-12 max-w-xl mx-auto">Everything your group needs to plan, decide, and book — without the drama.</p>
        <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {groupFeatures.map(f => (
            <div class="card-hover p-6">
              <div class="text-3xl mb-3">{f.icon}</div>
              <h3 class="font-display font-bold text-dark-500 mb-2">{f.title}</h3>
              <p class="text-sm text-gray-500">{f.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- How it works for groups -->
    <section class="section-padding bg-gray-50">
      <div class="container-narrow max-w-3xl text-center">
        <h2 class="text-3xl font-display font-bold text-dark-500 mb-10">How Group Planning Works</h2>
        <div class="space-y-8">
          {[
            { s: '1', t: 'Create Your Trip', d: 'Set destination, dates, and budget. AI generates an initial plan.' },
            { s: '2', t: 'Invite Your Crew', d: 'Share a link via WhatsApp, email, or social media. No app download needed.' },
            { s: '3', t: 'Collaborate & Vote', d: 'Everyone suggests activities, votes on options, and discusses in real-time.' },
            { s: '4', t: 'Lock & Book', d: 'The organizer finalizes the plan and books everything with one click.' },
          ].map(step => (
            <div class="flex items-start gap-6 text-left">
              <div class="w-12 h-12 rounded-full bg-gradient-to-br from-teal-500 to-primary-500 text-white flex items-center justify-center font-bold text-lg flex-shrink-0">{step.s}</div>
              <div>
                <h3 class="font-display font-bold text-dark-500 text-lg">{step.t}</h3>
                <p class="text-gray-500">{step.d}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <section class="section-padding bg-gradient-to-r from-primary-500 to-teal-500 text-white text-center">
      <div class="container-narrow">
        <h2 class="text-3xl font-display font-bold mb-6">Rally Your Crew</h2>
        <p class="text-white/80 text-lg mb-8">Start a group trip and start collaborating in minutes.</p>
        <a href="/#hero-input" class="btn-primary !bg-white !text-primary-500 hover:!bg-gray-100">Start Group Trip — Free</a>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>
